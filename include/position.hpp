//
// Created by plush-jill on 6/1/25.
//

#ifndef POSITION_HPP
#define POSITION_HPP


/*
class Position {
private:
    int m_x_;
    int m_y_;

public:
    explicit Position(const int x, const int y) : m_x_(x), m_y_(y) {};

    [[nodiscard]] int getX() const { return m_x_; }
    [[nodiscard]] int getY() const { return m_y_; }
    void setX(const int new_x) { m_x_ = new_x; }
    void setY(const int new_y) { m_y_ = new_y; }

    [[nodiscard]] bool operator==(const Position& other) const {
        return m_x_ == other.m_x_ && m_y_ == other.m_y_;
    }

    [[nodiscard]] bool operator!=(const Position& other) const {
        return !(*this == other);
    }
};
*/



#endif //POSITION_HPP
